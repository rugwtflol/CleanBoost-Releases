# CleanBoost Pro Stable Channel

This folder hosts the public update manifest used by CleanBoost Pro.

The live `update.json` file is published only after the matching installer has been built, hashed with SHA-256, uploaded to a GitHub Release, and tested.
